{
  "name": "repoai-backend",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@prisma/client": "^7.1.0",
    "cors": "^2.x",
    "dotenv": "^16.x",
    "express": "^4.x",
    "prisma": "^7.1.0"
  },
  "devDependencies": {
    "@types/express": "^4.x",
    "@types/node": "^20.x",
    "ts-node-dev": "^2.x",
    "typescript": "^5.x"
  }
}
