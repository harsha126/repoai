import { BrowserRouter } from "react-router-dom";
import AppRoutes from "./Route";

function App() {
    return (
        <BrowserRouter>
            <AppRoutes />
        </BrowserRouter>
    );
}

export default App;
